<?php /** * Copyright © 2013-2017 Magento, Inc. All rights reserved. * See COPYING.txt for license details. */ $objectManager = \Magento\Framework\App\ObjectManager::getInstance(); $cart = $objectManager->get('\Magento\Checkout\Model\Cart'); $priceHelper = $objectManager->create('Magento\Framework\Pricing\Helper\Data'); $totalQuantity = $cart->getQuote()->getItemsQty(); $subTotal = $cart->getQuote()->getSubtotal(); $attributes = $block->getCssClass() ? ' class="' . $block->getCssClass() . '"' : ''; $attr = $block->getAttributes(); if (!empty($attr)) { foreach ($block->getAttributes() as $attribute => $value) { $attributes .= ' ' . $attribute . '="' . $value . '"'; } }; echo '<' . $block->getTag() . $attributes . '>' . $block->getText() .'('.(int)$totalQuantity.' items)</' . $block->getTag() . '>'; ?> <table class="item-total-sec"><tr><th><?php echo __('Items Total')?></td><td><?php echo $priceHelper->currency($subTotal, true, false) ?></td></tr></table>